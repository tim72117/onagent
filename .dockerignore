# 避免把不必要 / 含密碼的檔案送進 build context(也加快建置)。
# 注意:backend/、apps/landing/、apps/console/ 必須保留(build 需要),
# 這裡只排除產物與機密。

# 機密:.env* 含 DB 連線字串/密碼、或本機專用的 localhost 網址設定,絕不進映像
# (env 改用 --env-file / Cloud Run 變數注入)。.env.local 特別重要:之前漏排除
# 導致 apps/console/.env.local 的 VITE_CONSOLE_API_URL=http://localhost:8080
# 被 Vite 直接烤進正式站的 console 前端 bundle 裡,讓瀏覽器把公開網站誤判成
# 想連本機、跳出 Local Network Access 權限警告。
.env
.env.*
**/.env
**/.env.*
backend/.env

# Go 執行期產物與本機二進位
backend/server.exe
backend/onagent.exe
**/*.exe
backend/logs/
backend/sessions/
backend/entries.jsonl

# node_modules 排除:Dockerfile 用 npm ci 在容器內安裝
**/node_modules/

# 前端 dist 也排除:Dockerfile 在容器內 build。注意 backend/cmd/server/web/{landing,console}/
# 的 checked-in placeholder index.html 不受影響,Dockerfile 會在複製真正 dist 前先 rm -rf 清掉它們。
apps/*/dist/

# 與部署無關的目錄
examples/
mockups/
docs/

# 版控與系統檔
.git/
.gitignore
**/.DS_Store
